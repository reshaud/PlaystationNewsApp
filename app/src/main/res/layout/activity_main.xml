<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.android.playstationnewsapp.MainActivity">

    <ListView
        android:id="@+id/news_list_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

    </ListView>

    <TextView
        android:id="@+id/empty_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="@+id/news_list_view"
        app:layout_constraintRight_toRightOf="@+id/news_list_view"
        app:layout_constraintTop_toTopOf="@+id/news_list_view"
        app:layout_constraintBottom_toBottomOf="@+id/news_list_view"
        android:textAppearance="?android:textAppearanceLarge"/>

    <ProgressBar
        android:id="@+id/progress_bar"
        style="@style/Widget.AppCompat.ProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="@+id/news_list_view"
        app:layout_constraintRight_toRightOf="@+id/news_list_view"
        app:layout_constraintTop_toTopOf="@+id/news_list_view"
        app:layout_constraintBottom_toBottomOf="@+id/news_list_view"
        android:indeterminate="true"/>

</android.support.constraint.ConstraintLayout>
